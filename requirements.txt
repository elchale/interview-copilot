# Audio capture
PyAudioWPatch>=0.2.12.6
numpy>=1.26.0

# Hotkeys & system tray
pynput>=1.7.6
pystray>=0.19.5
Pillow>=10.0.0

# Web server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.0.0

# Live call mode (Deepgram streaming STT)
websockets>=12.0

# AI providers
# 0.109+ required for output_config (structured outputs) and the web_search_20260209 tool used by live mode
anthropic>=0.109.0
openai>=1.50.0
google-genai>=1.0.0
httpx>=0.27.0

# Utilities
python-dotenv>=1.0.0

# Encryption (Windows)
pywin32>=306

# Build
pyinstaller>=6.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
